# Expose the rider's jump state so hover vehicles can read "space held" to
# climb. Space does not dismount in 26.1 (only sneak does), so it is a free
# vertical-thrust control.
public net.minecraft.world.entity.LivingEntity jumping
