# craftee texture & model tools

The Craftee armor's worn-armor textures are authored in
[Blockbench](https://www.blockbench.net/), a free 3D model and texture
editor for Minecraft. This directory holds the canonical Blockbench
project files:

```
craftee_armor_helmet.bbmodel      # head cube
craftee_armor_chestplate.bbmodel  # body + both arms
craftee_armor_boots.bbmodel       # boot cubes (legs region of humanoid layer)
craftee_armor_leggings.bbmodel    # leg cubes
```

One bbmodel per armor piece, so you can focus on whichever piece you're
editing. The first three (helmet / chestplate / boots) all share the
**same** PNG: `assets/craftee/textures/entity/equipment/humanoid/craftee.png`
— that single 64×32 sheet contains the helmet, chestplate, and boots
pixels in different UV regions. **Editing any of the three bbmodels and
saving the texture writes to the same PNG file.** Use the per-piece
split for visual focus, not isolation; if you want to repaint just the
helmet face you'd open the helmet bbmodel and Blockbench will only
show the head cube, but the underlying texture you're modifying is
shared with chestplate and boots.

The leggings bbmodel writes to a different PNG (humanoid_leggings layer):
`assets/craftee/textures/entity/equipment/humanoid_leggings/craftee.png`.

Each bbmodel embeds the relevant PNG as base64 so it opens self-contained
in Blockbench.

## Workflow

1. Install Blockbench from <https://www.blockbench.net/>.
2. Open one of the four bbmodels (double-click, or **File → Open** in
   Blockbench).
3. The 3D viewport shows the relevant cubes (humanoid: head/body/arms/boots;
   leggings: legs) with the current texture applied. Edit:
   - **Texture** — switch to the *Paint* tab, paint pixels in 2D or 3D.
   - **Cubes** — switch to the *Edit* tab; for armor we generally do not
     change cube geometry, only paint pixels.
4. Save the bbmodel: **File → Save** (overwrites the `.bbmodel` file).
5. Export the texture back into the resource pack:
   - In the **Textures** panel (right sidebar), right-click the texture
     thumbnail → **Save** (overwrites the linked PNG in
     `../src/main/resources/assets/craftee/textures/entity/equipment/...`).
6. Run `./gradlew :craftee:build && ./gradlew :craftee:runClient`
   to verify the change in-game.

## Regenerating the bbmodels

If the linked PNG changes outside Blockbench (for example,
`craftee/tools/generate_textures.py` is rerun with palette tweaks),
regenerate the bbmodels so their embedded base64 preview matches:

```bash
python3 craftee/tools/generate_bbmodels.py
```

This rewrites all four bbmodels with fresh UUIDs and re-embedded PNG
bytes. Re-running is safe (idempotent for shape; UUIDs differ each run,
but Blockbench treats them as opaque identifiers).

## Why Blockbench?

- Visual feedback on which UV slot maps to which face of which cube.
- Faster iteration than hand-editing pixel coordinates in Python.
- Standard tool for Minecraft model authoring; bbmodel format is
  portable and well-supported.

## Notes

- The bbmodels use `model_format: modded_entity` with `box_uv: true` and
  resolution 64×32 — the standard humanoid armor sheet layout. Cube
  pivots match vanilla `HumanoidModel` so the in-Blockbench preview
  matches what the game actually renders on a player wearing the armor.
- The cubes in `craftee_armor_boots.bbmodel` use UV (0, 16) on the
  humanoid texture. That UV slot is shaped like a leg in the texture
  but the renderer maps it onto the boots region of the in-game player,
  which is why the cubes are named `right_boot` / `left_boot` rather
  than `right_leg` / `left_leg`.
- `craftee/build.gradle` excludes `**/*.bbmodel` from the runtime jar
  (via `processResources`), so these files live alongside the source but
  never ship with the mod.
- These bbmodels are NOT regenerated by `generate_textures.py`. Run
  `generate_bbmodels.py` separately if you want to re-embed updated
  PNG bytes after a palette change.
